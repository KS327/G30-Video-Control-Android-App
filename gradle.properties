# Enable AndroidX because this project uses androidx.* dependencies.
android.useAndroidX=true

# Keep enabled because some proprietary/older AAR files may still reference legacy android.support.* classes.
android.enableJetifier=true

# Remove this line after upgrading to an Android Gradle Plugin version that officially supports compileSdk 35.
android.suppressUnsupportedCompileSdk=35

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
